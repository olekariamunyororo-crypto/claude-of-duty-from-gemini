/**
 * Optional observability hook. Ships as a no-op so the game stays fully
 * offline-capable; wire @sentry/react here if you deploy a hosted build.
 */
export function initSentry(): void {
  // noop
}
